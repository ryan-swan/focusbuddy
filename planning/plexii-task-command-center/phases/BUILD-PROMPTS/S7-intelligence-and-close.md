# S7 — Intelligence-Light, Regression Guard, G6 Close

> **DEC-018 rider (A-6):** the G6 merge-readiness preconditions gain the D4 integration
> doctrine: agents write work items ONLY through the workItems module (F008) — a
> parallel write path silently breaks every invariant (projection, leaf, scope, C2,
> CI locks). One paragraph, handed to Caleb with the §8 R016 preconditions.

**Class:** ADDITIVE + system-wide verification · **Needs:** S4 (nudges) · **Risk:** LOW
build, HIGH judgment — this stage decides whether the whole layer feels helpful or naggy.

**Mission:** the restrained intelligence tier (SPEC-022/023/024) ships with its
one-directional feeders and narrow nudge defaults; then the ENTIRE build is regression-
proven and the G6 gate closes with an honest system-state report.

## Read first
- ARCHITECTURE §6 SPEC-018/019 (already-built ranker this extends) + §7 S7 row + §5's
  "content restraint defaults" note · SPEC-022/023/024 in
  [analysis/13-SPEC-001-A1.md](../../analysis/13-SPEC-001-A1.md) ·
  QUALITY-FRAMEWORK.md G6 definition

## Build items
1. **SPEC-022/023/024 intelligence-light:** the P0 subset per the compiled spec —
   feeders that READ from desks/docs/calendar into attention signals, strictly
   one-directional (attention never writes back into feeder domains at v1).
2. **Nudge restraint defaults (the §5 content half):** narrow triggers only
   (approaching-deadline, explicit-ask aging, delegation unanswered); everything else
   surfaces passively in queues. Defaults conservative; a config surface can widen later.
3. **The regression guard, system-wide:** whole-suite + typecheck + the S1 blast-radius
   manual pass + every stage's live proof re-executed once on the final build.
4. **G6 close-out:** the honest ledger — every SPEC item: SHIPPED / PARTIAL (what
   remains) / DEFERRED (P1/P2, where registered). Every accepted residual from §8
   restated. The P1 owed-list consolidated (routed-trash retention rule, shared-refresh
   widening, `nodeSharedRoot` partition fix, migrated-peer confirmation, post-migration
   kind test if deferred, 409-zombie confirmation).

## Adversarial / verify
- **Feeder one-directionality:** grep + unit — no attention-module import writes into
  desk/plan/doc domains; a mutation attempt is a type error or refusal.
- **Nudge restraint fixture:** a busy fixture week produces ≤ the budgeted nudge count;
  the passive queues carry the rest.
- **Whole-suite + live pass:** the full loop from S5's e2e re-run on the final build;
  four-theme spot check; sync two-device spot check (poll + live path).

## Close
G6 verdict written to ACTIVE-MISSION + a final HANDOFFS/phase-6-close.md · the merge-
readiness preconditions from §8 (R016) restated for the eventual PR to Caleb — defensive
branches upstream + observed firing; `schema_epoch` gate live; else `workItems.enabled`
ships opt-in-only · operator briefing: what shipped, what's P1, what needs his click.
