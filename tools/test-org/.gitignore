# Local test-org runtime state: signal DB, per-account app profiles, session
# tokens, launched-window pids, signal log. Never committed — regenerated by
# `run.mjs setup` and wiped by deleting this directory.
.state/
